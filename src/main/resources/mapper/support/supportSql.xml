<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.fc.support.supportDao.SupportDao">
    <!--공지사항 모두 조회-->
    <select id="noticeAllList" resultType="com.example.fc.support.supportVo.SupportVo">
        select notice_board, title, detail, sortation, create_Date
        from notice;
    </select>

    <!-- 공지사항 등록 -->
    <insert id="noticeInsert" parameterType="com.example.fc.support.supportVo.SupportVo">
        insert into notice(title, detail, sortation, create_Date)
        values (#{title}, #{detail}, #{sortation}, now());
    </insert>

    <!--  공지사항 상세보기-->
    <select id="noticeOneList" resultType="com.example.fc.support.supportVo.SupportVo">
        select notice_board,
               title,
               detail,
               sortation,
               create_date
        from notice
        where notice_board = #{noticeBoard}


    </select>
    <!-- 공지사항 수정 modify -->
    <update id="noticeModify" parameterType="com.example.fc.support.supportVo.SupportVo">
        update notice
        set title=#{title},
            detail=#{detail},
            sortation=#{sortation}
        where notice_board = #{noticeBoard};

    </update>
    <!--    공지사항 삭제-->
    <delete id="noticeDelete">
        delete from notice where notice_board = #{noticeBoard};
    </delete>
</mapper>